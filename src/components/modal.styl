.rc-dialog {
	.overlay {
		position: fixed;
		top: 0;
		right: 0;
		bottom: 0;
		left: 0;
		transition: all 0.15s linear;
		z-index: 1000;
		background-color: rgba(0,0,0,.8);
		overflow-y: auto;
		display: flex;
		align-items: center;
		justify-content: center;
    }
}